const { io } = require("..");
io.of("conversation").on(connection,socket=>{

})

//Create conversation
const creatConversation = (req,res)=>{
    const user = req.user
    
}


//List Conversation of user

// Add Message to conversation


//Delete conversation


//Add user to conversation

//Remove user from conversation

//Todo: Add sockets


module.exports = {creatConversation}